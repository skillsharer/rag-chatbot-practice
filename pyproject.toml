[project]
name = "rag-chatbot-practice"
version = "1.0.0"
description = "A rag chatbot based on Langgraph."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.61.1",
]
