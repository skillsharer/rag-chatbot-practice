[project]
name = "rag-chatbot-practice"
version = "1.0.0"
description = "A rag chatbot based on Langgraph."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pymupdf>=1.28.2",
    "python-dotenv>=1.2.2",
]
